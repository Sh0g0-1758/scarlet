#include <codegen/common.hh>

namespace scarlet {
namespace codegen {
void Codegen::optimize(scarcmd &cmd) {
  (void)cmd;
  for (auto function : scar.get_elems()) {
    // body
  }
}
} // namespace codegen
} // namespace scarlet